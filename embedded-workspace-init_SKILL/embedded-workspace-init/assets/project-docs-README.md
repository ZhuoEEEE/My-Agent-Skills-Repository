# 项目知识

这里只保存经确认、跨任务有效的项目事实。

- `PRODUCT.md`：长期产品目标、用户和能力边界。
- `SOLUTION.md`：当前有效方案、系统划分和约束。
- `ROADMAP.md`：阶段、依赖、范围、退出条件和下一步。
- `decisions/`：可追溯的重要决策。
- `versions/`：版本目标、包含/不包含范围与完成标准。

候选结论先留在 workstream；只有真实内容存在且用户确认后才创建或更新正文。

